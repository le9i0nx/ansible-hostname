---

hostname: "{{ inventory_hostname }}"
hostname_short: "{{ inventory_hostname_short }}"

hostname_hosts:
    - { ip: "127.0.0.1", name: "{{hostname}}{% if inventory_hostname != hostname_short %}\t{{hostname_short}}{% endif %}\tlocalhost" }

hostname_hosts_add: []
